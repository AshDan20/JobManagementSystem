spring:
  jpa:
    open-in-view: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create
    show-sql: false
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
  quartz:
    job-store-type: memory
  output:
    ansi:
      enabled: ALWAYS